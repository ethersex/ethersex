CONTROL_START
  
  PIN_INPUT(TASTER)
  PIN_PULLUP(TASTER)
  PIN_OUTPUT(LED)

  ACTION(new)
      TIMER_START(new)
      PIN_SET(LED);
      TIMER_WAIT(new, 20)
      PIN_CLEAR(LED);
  ACTION_END(new)

  ON CLOCK_MIN == 5 DO ACTION_START(new) END

CONTROL_END

